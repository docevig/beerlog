-- Настоящий тип снимка. Telegram-у файл уходит нейтральными байтами: имя
-- с расширением .webp он относит к стикерам и возвращает не тем полем,
-- из-за чего снимок терялся сразу после отправки. Раз тип не восстановить
-- из ответа файлового сервера, держим его у себя.
ALTER TABLE photos ADD COLUMN mime TEXT;
