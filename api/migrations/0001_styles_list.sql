-- Витрине не хватало САМИХ стилей: сервер знал только их количество,
-- поэтому сравнить вкусы было не с чем. Храним компактный список кодов,
-- без дат и объёмов — это не история, а срез вкуса за месяц.
ALTER TABLE totals ADD COLUMN styles_list TEXT;
